#배열의 크기N, 더하는 횟수M, 반복횟수 K를 입력받기
n, m, k = map(int, input().split())
#N개의 자연수 입력받기
array = list(map(int, input().split()))

#리스트 정렬후 가장 큰수와 두번째 큰수 뽑기
array.sort()
first = array[n-1]
second = array[n-2]

result = 0

#반복문을 이용하여 계산하기
while True:
    for i in range(k):
        if m == 0:
            break
        else:
            result += first
            m -= 1
    if m == 0:
        break
    else:
        result += second
        m -= 1

print(result)

문제해결:
'가장 큰 수 k번 더하고 그 다음 큰 수 한번 더 하는 연산 반복하기'
