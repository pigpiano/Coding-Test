#행의 개수n 과 열의 개수 m을 공백으로 구분하여 입력받기.
n, m = map(int, input().split())

result = 0
for i in range(n):
#한 줄씩 입력받아 확인하기
    card_number = list(map(int, input().split()))
# 가장 작은 수 뽑기
    min_value = min(card_number)
# 가장 작은 수들 중에서 가장 큰 수 뽑기
    result = max(min_value, result)
# 최종 결과 값 출력
print(result)
